---
description: Wrap up this working session so it can be closed and archived
---

Wind this session down. It is finished work, not work in progress — the point
is to leave nothing behind that a future reader would have to reconstruct.

1. Report the state of the tree: `git status --short` and `git log --oneline -5`.
   Say plainly whether anything is uncommitted, and whether that is deliberate.
2. Remove the debris this session created that is not meant to be kept —
   scratch scripts, temp output, commented-out experiments, stray debug logging.
   List what you removed. Do not touch anything you are unsure about; name it
   instead and let me decide.
3. State in two or three lines what this session actually accomplished, and
   anything left unfinished that I would want to know about later.

Do not commit, push, or open a PR unless I ask.
